{% extends 'base.html.twig' %}

{% block title %}Pour se connecter, c'est ici!{% endblock %}

{% block body %}
    <h2>Connexion</h2>
    {{ form(nouveauprojet) }}
    {{ form(gestionprojet) }}
    {{ form(nouvelledepense) }}
    {{ form(remboursements) }}

{% endblock %}